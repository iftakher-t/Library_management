const User = require('../models/Profile')

const userNameChangController = async (req, res)=>{
    try{
       const  = await
    }
    catch(err){

    }
}

const passwordChangController = async (req, res)=>{
    try{
       const  = await
    }
    catch(err){

    }
}

const profilePictureChangController = async (req, res)=>{
    try{
       const  = await
    }
    catch(err){

    }
}


module.exports = {
    pictureChangController
}